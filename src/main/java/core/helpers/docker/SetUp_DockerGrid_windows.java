package core.helpers.docker;import org.testng.annotations.AfterTest;import org.testng.annotations.BeforeTest;import org.testng.annotations.Test;import java.io.IOException;public class SetUp_DockerGrid_windows{    //@BeforeTest    @Test(priority = 1)    void startDockerGrid() throws IOException, InterruptedException    {        Runtime.getRuntime().exec("cmd /c start docker_grid_start_windows.bat");// executing cmd command        Thread.sleep(5000);    }    //@AfterTest    @Test(priority = 2)    void  stopDockerGrid() throws IOException, InterruptedException    {        Runtime.getRuntime().exec("cmd /c down docker_grid_start_windows.bat");//        Thread.sleep(5000);        Runtime.getRuntime().exec("taskkill /f /im cmd.exe"); // close all command prompts    }}